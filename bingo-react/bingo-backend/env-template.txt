# MongoDB Atlas Configuration
# Replace <username>, <password>, and <cluster-url> with your actual values
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/bingo-game?retryWrites=true&w=majority

# JWT Secret (keep this secret!)
JWT_SECRET=epam-bingo-secret-key-2024-super-secure

# Server Configuration
PORT=5000
CORS_ORIGIN=http://localhost:5173

# Example Atlas URI format:
# MONGODB_URI=mongodb+srv://myuser:mypassword@cluster0.abc123.mongodb.net/bingo-game?retryWrites=true&w=majority
